# ЛР5 / Множество


## Собрать и запустить:

`. ./run.sh`  или  `source ./run.sh`

## Ручная сборка
### Команды сборки

* `cmake -B .build`
* `make -C .build`


### Команда запуска (в `./bin`)

`./lab_5`