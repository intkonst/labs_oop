# ЛР3 / Дроби

## Собрать и запустить

`. ./run.sh` или `source ./run.sh`

*все флаги скрипта передаются напрямую к исполняемому файлу проекта*

## Команды сборки
* `cmake -B .build`
* `make -C .build`


## Команды запуска
Режим демонстрации:
`./lab_3`

Интерактивный режим:
`./lab_3 -i`